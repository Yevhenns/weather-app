import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  wrapper: {
    width: "100%",
    padding: 16,
    gap: 10,
  },
  weatherWrapper: {
    gap: 10,
  },
  heading: {
    textAlign: "center",
  },
});
